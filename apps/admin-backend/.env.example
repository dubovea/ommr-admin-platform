# HTTP
HOST=0.0.0.0
PORT=4000
JSON_BODY_LIMIT=20mb

# CORS is permissive by default for direct API access.
# Use * for any origin, or comma-separated origins for stricter deployments.
CORS_ORIGIN=*

# Database
DATABASE_URL=postgres://ommr:ommr_password@localhost:5433/ommr_admin
